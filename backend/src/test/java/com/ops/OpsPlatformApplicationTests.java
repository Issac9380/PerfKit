package com.ops;

/**
 * Unit tests for the application.
 * Run only service layer unit tests to avoid Spring context initialization issues.
 */
class OpsPlatformApplicationTests {

    // Note: Full integration tests require SQLite in-memory database setup
    // Run AuthServiceTest, AiConfigServiceTest, VersionServiceTest for unit tests
}
